#include <stdio.h>
int main ()
{
int h, l;
printf("enter the height of triangle", &h);
scanf("%d",&h);
printf("enter the length of base of triangle", &l);
scanf("%d",&l);
printf("area of circle =%.2f", (float)l*h/2);

}
